---
description: HELM Snapshot door-opener: one file, one screen, one language, two-page readout
argument-hint: <csv-or-xlsx> [prospect-name]
---
Run `prompts/sp-09-helm-snapshot.md` on `$ARGUMENTS`. Cap at 2,000 rows. One headline, three support metrics, one language via the matching table, one 16:9 screen with Prime, Support, Context, and Status Rail only. Produce `snapshot.html` and a two-page branded readout with the `bhil-docx` skill. No scaffold. End with the upsell path to Core Build.
