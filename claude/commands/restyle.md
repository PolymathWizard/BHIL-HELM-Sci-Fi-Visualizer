---
description: Swap the token set to another catalog language, keep all bindings, re-run contrast
argument-hint: <language-slug>
---
Confirm `$ARGUMENTS` is a slug in `data/canonical/catalog.json`. If the user named a protected property instead, translate to the nearest catalog language and say so. Copy `tokens/$ARGUMENTS.css` into the engagement, repoint the artifact's token link, apply the new language's hierarchy device to zone frames, and keep every data binding untouched. Re-run the Narrative Tax Register for the new language (helm-stylist). Then delegate to helm-qa to run `python3 tools/contrast.py $ARGUMENTS` on every pair and report any below floor. Never carry glow onto text.
